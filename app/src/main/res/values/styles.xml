<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/darkblue</item>
        <item name="colorPrimary">@color/colorPrimaryBlue</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="PopupAnimation">
        <item name="android:windowEnterAnimation">@anim/pop_up</item>
        <item name="android:windowExitAnimation">@anim/pop_up_out</item>
    </style>
    <style name="NoteAnimation">
        <item name="android:windowEnterAnimation">@anim/note_in</item>
        <item name="android:windowExitAnimation">@anim/note_out</item>
    </style>

<!--    <style name="PauseDialog" parent="@android:style/Theme.Light">-->
<!--        <item name="android:windowAnimationStyle">@style/PauseDialogAnimation</item>-->
<!--    </style>-->
    <style name="PauseDialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

    <style name="MyEditText" parent="android:Widget.EditText">
        <item name="background">@drawable/custom_edittext</item>

    </style>
    <style name="Popup_window_anim">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>


</resources>
